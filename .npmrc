# Required: @langchain/community@1.1.27 demands @langchain/core@^1.1.38 but
# root pins core@^0.3.73 to match @langchain/openai@0.6.x + langchain@0.3.x.
# Resolve via legacy-peer-deps until langchain ecosystem realigns (track at
# https://github.com/langchain-ai/langchainjs for community→0.3 backport or
# upgrade plan to core@1.x).
legacy-peer-deps=true
