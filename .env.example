# ================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# All production values should be set in Vercel dashboard
# ================================================

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/disaster_recovery"

# Supabase (Optional - if using Supabase)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32

# Stripe Configuration
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Stripe Price IDs (create these in Stripe Dashboard)
STRIPE_APPLICATION_FEE_PRICE_ID="price_..."
STRIPE_JOINING_FEE_PRICE_ID="price_..."
STRIPE_SUB_MONTH_1_PRICE_ID="price_..."
STRIPE_SUB_MONTH_2_PRICE_ID="price_..."
STRIPE_SUB_MONTH_3_PRICE_ID="price_..."
STRIPE_SUB_REGULAR_PRICE_ID="price_..."

# Email (SMTP)
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your_email@example.com
EMAIL_SERVER_PASSWORD=your_password
EMAIL_FROM=noreply@disasterrecovery.com.au

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Disaster Recovery Australia"

# AI Services (Get from respective dashboards)
OPENROUTER_API_KEY=sk-or-v1-...
ANTHROPIC_API_KEY=sk-ant-api03-...
OPENAI_API_KEY=sk-...

# AWS S3 (for document uploads)
AWS_ACCESS_KEY_ID="..."
AWS_SECRET_ACCESS_KEY="..."
AWS_REGION="ap-southeast-2"
AWS_S3_BUCKET="nrp-contractor-documents"

# SMS (Twilio)
TWILIO_ACCOUNT_SID="..."
TWILIO_AUTH_TOKEN="..."
TWILIO_PHONE_NUMBER="+61..."

# Google Maps API (for location services)
GOOGLE_MAPS_API_KEY="..."

# ElevenLabs API for Voice Narration
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
# Voice ID: WLKp2jV6nrS8aMkPPDRO (Professional voice selected)
NEXT_PUBLIC_ELEVENLABS_API_KEY="..."

# ABN Lookup (Australian Business Register)
ABN_LOOKUP_GUID="..."

# ================================================
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit actual API keys to git
# 2. Use Vercel environment variables for production
# 3. Rotate keys immediately if exposed
# 4. Use different keys for dev/staging/production
# ================================================