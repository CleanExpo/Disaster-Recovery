'use client';

/**
 * @deprecated Import directly from '@/components/ui/Spinner' instead.
 * This barrel exists to avoid breaking imports during the DR-751 migration.
 */
export {
  Spinner as LoadingSpinner,
  PageLoader,
  LoadingDots,
  ServiceCardSkeleton,
  HeroSkeleton,
} from '@/components/ui/Spinner';
