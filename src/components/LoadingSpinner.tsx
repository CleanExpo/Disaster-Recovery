'use client';

/**
 * @deprecated Import directly from '@/components/ui/Spinner' instead.
 * This re-export exists to avoid breaking imports during the DR-751 migration.
 */
export { Spinner as default, LoadingSkeleton, LoadingDots } from '@/components/ui/Spinner';
